extends layout

block content
  h1= title
  p Welcome to #{title}

  form(name="generate", action="/create", method="post")
    p
      label(for="json") json-data:
        textarea(id="json", name="json", rows="10" ,cols="100")

    input(type="submit", value="ceate")